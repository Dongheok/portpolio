const data = [
    { key: '/', value: '' },
    { key: '/project', value: 'project' },
    { key: '/profile', value: 'profile' },
];

const model = {
    pageKey: '',
    data: data,
    isLoading: false,
};

export default model;
